# coding: utf-8

"""
    API Reference

    Detailed API reference documentation for Visier APIs. Includes all endpoints, headers, path parameters, query parameters, request body schema, response schema, JSON request samples, and JSON response samples.

    The version of the OpenAPI document: 22222222.99201.2451
    Contact: alpine@visier.com
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictStr, field_validator
from typing import Any, ClassVar, Dict, List, Optional
from typing import Optional, Set
from typing_extensions import Self

class QueryAxisSortDTO(BaseModel):
    """
    Sort the axis members returned in the response by type and direction.
    """ # noqa: E501
    sort_type: Optional[StrictStr] = Field(default=None, description="The order in which to return axis members in the response. Valid values:  - `CONFIGURED`: Sorts axis members by their configuration:    - For parent-child dimensions, sorts axis members alphabetically by name.    - For all other dimensions, sorts axis members by their configured order in Studio.    - Unknown members are always placed at the end of the axis.  - `METRIC_VALUE`: Sorts axis members by their metric values. Applicable if there is only one query axis and only one time axis member.", alias="sortType")
    sort_direction: Optional[StrictStr] = Field(default=None, description="The direction in which to return axis members in the response. Valid values:  - `SORT_ASCENDING`: Sorts axis members in ascending order.  - `SORT_DESCENDING`: Sorts axis members in descending order.", alias="sortDirection")
    __properties: ClassVar[List[str]] = ["sortType", "sortDirection"]

    @field_validator('sort_type')
    def sort_type_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['CONFIGURED', 'METRIC_VALUE']):
            raise ValueError("must be one of enum values ('CONFIGURED', 'METRIC_VALUE')")
        return value

    @field_validator('sort_direction')
    def sort_direction_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['SORT_ASCENDING', 'SORT_DESCENDING']):
            raise ValueError("must be one of enum values ('SORT_ASCENDING', 'SORT_DESCENDING')")
        return value

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of QueryAxisSortDTO from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of QueryAxisSortDTO from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "sortType": obj.get("sortType"),
            "sortDirection": obj.get("sortDirection")
        })
        return _obj


